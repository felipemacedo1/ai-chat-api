spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chatai_dev
    username: postgres
    password: postgres
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.chatai: DEBUG
    org.springframework.security: DEBUG
